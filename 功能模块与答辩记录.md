# 校园跑腿代取快递平台 — 功能模块与答辩记录

---

## 一、项目概述

**项目名称**：校园跑腿代取快递平台  
**技术栈**：
| 层级 | 技术 |
|------|------|
| 后端框架 | Spring Boot 3.5.16 + MyBatis-Plus 3.5.9 |
| 数据库 | MySQL 8.0 + Redis |
| 前端框架 | Vue 3 + TypeScript + Element Plus |
| API文档 | SpringDoc OpenAPI (Swagger) |
| 构建工具 | Maven (后端) / Vite (前端) |
| Java版本 | JDK 21 |

**角色体系**：普通用户（role=0）、跑腿员（role=1）、管理员（role=2）

---

## 二、功能模块详述

### 模块 1：用户管理模块（User）

**数据库表**：`user`

| 功能 | 说明 |
|------|------|
| 用户注册 | 学号 + 密码注册，校验学号唯一性 |
| 用户登录 | 学号 + MD5密码登录，返回UUID Token |
| 修改密码 | 验证原密码后修改 |
| 余额充值 | 账户余额充值，支持小数金额 |
| 个人中心 | 查看/编辑个人信息（昵称、手机号、宿舍楼栋等） |
| 头像上传 | 支持文件上传和Base64两种方式，存储到本地磁盘 |
| 用户列表 | 分页查询、按学号/昵称模糊搜索、按状态筛选 |
| 启用/禁用 | 管理员可禁用违规用户 |
| 逻辑删除 | MyBatis-Plus逻辑删除，数据不物理删除 |

**后端文件**：
- `entity/User.java` — 实体类
- `mapper/UserMapper.java` — 数据访问层
- `service/UserService.java` + `service/impl/UserServiceImpl.java` — 业务逻辑层
- `controller/UserController.java` — 接口控制器（登录/注册/CRUD/头像上传/充值/改密）

---

### 模块 2：快递点管理模块（ExpressPoint）

**数据库表**：`express_point`

| 功能 | 说明 |
|------|------|
| 快递点列表 | 查询所有启用的快递点（按sort_order排序） |
| 快递点管理 | 管理员可增删改查快递点 |
| 启用/禁用 | 管理员可禁用快递点使其不在列表显示 |
| 排序权重 | sort_order字段控制显示顺序 |

**后端文件**：
- `entity/ExpressPoint.java`
- `mapper/ExpressPointMapper.java`
- `service/ExpressPointService.java` + `impl/ExpressPointServiceImpl.java`
- `controller/ExpressPointController.java`

---

### 模块 3：跑腿员管理模块（Runner）

**数据库表**：`runner`

| 功能 | 说明 |
|------|------|
| 申请跑腿员 | 普通用户提交真实姓名、手机号申请成为跑腿员 |
| 审核跑腿员 | 管理员审核（通过/拒绝），填写审核备注 |
| 跑腿员列表 | 分页查询，按审核状态/启用状态筛选 |
| 启用/禁用 | 管理员可禁用违规跑腿员 |
| 评分统计 | 平均评分、累计接单数、累计收益自动更新 |
| 跑腿员排行 | 按接单量/评分排序展示 |

**后端文件**：
- `entity/Runner.java`
- `mapper/RunnerMapper.java`
- `service/RunnerService.java` + `impl/RunnerServiceImpl.java`
- `controller/RunnerController.java`

---

### 模块 4：任务发布模块（Task）

**数据库表**：`task`

| 功能 | 说明 |
|------|------|
| 发布任务 | 用户填写快递点、取件码、送达地址、跑腿费、是否加急、备注 |
| 我的任务 | 查看自己发布的所有任务及其状态 |
| 任务列表 | 分页查询，管理员可查看全部任务 |
| 任务取消 | 待接单状态的任务可取消 |
| 可用任务 | 跑腿员端展示所有待接单任务（抢单大厅） |
| 筛选过滤 | 按快递点、跑腿费区间筛选可用任务 |
| 乐观锁 | version字段实现并发抢单控制 |

**任务状态流转**：`0=待接单 → 1=已接单 → 2=已取消`

**后端文件**：
- `entity/Task.java`
- `mapper/TaskMapper.java`
- `service/TaskService.java` + `impl/TaskServiceImpl.java`
- `controller/TaskController.java`

---

### 模块 5：订单管理模块（Order）

**数据库表**：`order`

| 功能 | 说明 |
|------|------|
| 抢单 | 跑腿员抢单，校验任务状态、跑腿员审核状态、不能抢自己的任务 |
| 状态流转 | 已接单→已取件→配送中→已送达→待评价→已完成 |
| 我的接单 | 跑腿员查看自己的所有订单 |
| 订单列表 | 管理员查看全部订单，支持按状态/跑腿员/用户筛选 |
| 确认收货 | 用户确认收到快递 |
| 订单完成 | 双方互评后订单完成 |
| 乐观锁 | version字段保证订单状态更新并发安全 |
| 自动推送通知 | 每个状态变更自动推送通知给相关用户 |
| 订单日志 | 每次状态变更自动记录操作日志 |

**订单状态流转**：
```
0=已接单 → 1=已取件 → 2=配送中 → 3=已送达 → 4=待评价 → 5=已完成
```

**后端文件**：
- `entity/Order.java`
- `mapper/OrderMapper.java`
- `service/OrderService.java` + `impl/OrderServiceImpl.java`
- `controller/OrderController.java`（核心业务控制器，包含抢单、状态流转、确认收货、完成订单）

---

### 模块 6：订单日志模块（OrderLog）

**数据库表**：`order_log`

| 功能 | 说明 |
|------|------|
| 状态变更记录 | 记录每次订单状态变更的 from_status → to_status |
| 操作人追踪 | 记录操作人ID和角色（用户/跑腿员/管理员） |
| 备注说明 | 记录操作备注 |

**后端文件**：
- `entity/OrderLog.java`
- `mapper/OrderLogMapper.java`
- `service/OrderLogService.java` + `impl/OrderLogServiceImpl.java`
- `controller/OrderLogController.java`

---

### 模块 7：评价模块（Evaluation）

**数据库表**：`evaluation`

| 功能 | 说明 |
|------|------|
| 用户评跑腿员 | 订单完成后用户给跑腿员打分（1-5星）+ 文字评价 + 标签 |
| 跑腿员评用户 | 跑腿员给用户打分 |
| 自动评价 | 送达后超时未评价，系统自动给5星好评（定时任务每30秒检查） |
| 评价查询 | 按订单ID、评价人、被评人查询评价 |
| 评分更新 | 评价后自动更新跑腿员平均分 |

**评价类型**：`type=0 用户评跑腿员`，`type=1 跑腿员评用户`

**后端文件**：
- `entity/Evaluation.java`
- `mapper/EvaluationMapper.java`
- `service/EvaluationService.java` + `impl/EvaluationServiceImpl.java`
- `controller/EvaluationController.java`
- `service/AutoEvaluationService.java`（定时任务，每30秒自动评价超时订单）

---

### 模块 8：结算模块（Settlement）

**数据库表**：`settlement`

| 功能 | 说明 |
|------|------|
| 自动结算 | 评价完成后自动结算跑腿费到跑腿员余额 |
| 结算列表 | 管理员查看全部结算记录 |
| 跑腿员收益 | 跑腿员查看自己的收益明细 |
| 结算状态 | 待结算/已结算/已退款 |
| 退款处理 | 管理员可执行退款操作 |

**后端文件**：
- `entity/Settlement.java`
- `mapper/SettlementMapper.java`
- `service/SettlementService.java` + `impl/SettlementServiceImpl.java`
- `controller/SettlementController.java`

---

### 模块 9：消息模块（Message）

**数据库表**：`message`

| 功能 | 说明 |
|------|------|
| 发送消息 | 用户之间私信沟通 |
| 聊天记录 | 查询两人之间的聊天记录 |
| 未读消息 | 查询某用户的未读消息列表和数量 |
| 已读标记 | 单条标记已读/全部标记已读 |
| 消息角标 | 前端30秒轮询刷新未读消息数量 |

**后端文件**：
- `entity/Message.java`
- `mapper/MessageMapper.java`
- `service/MessageService.java` + `impl/MessageServiceImpl.java`
- `controller/MessageController.java`

---

### 模块 10：通知模块（Notification）

**数据库表**：`notification`

| 功能 | 说明 |
|------|------|
| 系统通知 | 自动推送：任务被接、状态变更、结算、评价等通知 |
| 通知列表 | 按用户ID查询通知 |
| 未读数量 | 查询未读通知数量（前端角标） |
| 已读标记 | 单条标记已读/全部标记已读 |
| 通知类型 | TASK_ASSIGNED / STATUS_CHANGED / SETTLEMENT / ANNOUNCEMENT 等 |

**后端文件**：
- `entity/Notification.java`
- `mapper/NotificationMapper.java`
- `service/NotificationService.java` + `impl/NotificationServiceImpl.java`
- `controller/NotificationController.java`

---

### 模块 11：交易流水模块（TransactionRecord）

**数据库表**：`transaction_record`

| 功能 | 说明 |
|------|------|
| 流水记录 | 所有资金变动自动记录 |
| 交易类型 | 充值(0)/支付跑腿费(1)/收入跑腿费(2)/提现(3)/退款(4) |
| 流水查询 | 按用户ID、交易类型查询，支持分页 |

**后端文件**：
- `entity/TransactionRecord.java`
- `mapper/TransactionRecordMapper.java`
- `service/TransactionRecordService.java` + `impl/TransactionRecordServiceImpl.java`
- `controller/TransactionRecordController.java`

---

### 模块 12：前端页面模块

#### 12.1 通用页面
| 页面 | 路由 | 说明 |
|------|------|------|
| 登录/注册 | `/login` | 学号密码登录，新用户注册 |
| 个人中心 | `/user/profile` | 查看编辑个人信息、修改密码、上传头像、充值余额、申请跑腿员 |
| 消息通知 | `/user/messages` | 查看系统通知和私信消息 |
| 评价管理 | `/user/evaluations` | 查看发出的评价和收到的评价 |

#### 12.2 用户端（普通用户）
| 页面 | 路由 | 说明 |
|------|------|------|
| 发布任务 | `/user/task-publish` | 选择快递点、输入取件码、设置跑腿费、是否加急 |
| 我的任务 | `/user/my-tasks` | 查看已发布任务列表和状态，可取消待接单任务 |

#### 12.3 跑腿员端
| 页面 | 路由 | 说明 |
|------|------|------|
| 抢单大厅 | `/runner/grab-hall` | 查看所有可接任务，按快递点/费用筛选，一键抢单 |
| 我的接单 | `/runner/my-orders` | 查看接单列表，更新配送状态（已取件→配送中→已送达） |
| 收益结算 | `/runner/income` | 查看收益统计和结算明细 |
| 我收到的评价 | `/runner/evaluations` | 查看用户对自己的评价 |

#### 12.4 管理端（管理员）
| 页面 | 路由 | 说明 |
|------|------|------|
| 用户管理 | `/admin/users` | 用户列表、搜索、启用/禁用 |
| 跑腿员管理 | `/admin/runners` | 跑腿员列表、审核（通过/拒绝）、启用/禁用 |
| 任务管理 | `/admin/tasks` | 全部任务列表查看 |
| 订单管理 | `/admin/orders` | 全部订单列表查看，按状态筛选 |
| 结算管理 | `/admin/settlements` | 结算记录、结算/退款操作 |
| 数据统计 | `/admin/statistics` | 仪表盘：用户数、跑腿员数、任务数、订单数、结算总额、跑腿员排行 |

#### 12.5 前端架构
| 模块 | 说明 |
|------|------|
| 路由守卫 | `router/index.ts` — 基于Token的登录校验 + 角色权限校验 |
| 状态管理 | `stores/user.ts` — Pinia管理用户登录状态和角色 |
| API封装 | `api/request.ts` — Axios封装，统一拦截器和错误处理 |
| 布局组件 | `layout/MainLayout.vue` — 侧边栏+顶栏+内容区，根据角色显示不同菜单 |

---

## 三、技术亮点

| 亮点 | 说明 |
|------|------|
| **并发抢单控制** | Task表和Order表使用 `version` 乐观锁字段，防止多人同时抢同一单 |
| **自动评价机制** | `@Scheduled` 定时任务每30秒扫描已送达超1分钟未评价的订单，自动5星好评 |
| **完整状态流转** | 订单6种状态（已接单→已取件→配送中→已送达→待评价→已完成）+ 全程日志记录 |
| **角色权限控制** | 3种角色（用户/跑腿员/管理员），前端路由守卫 + 后端接口权限隔离 |
| **通知推送** | 每个业务节点（抢单、状态变更、评价、结算）自动推送系统通知 |
| **逻辑删除** | 所有核心表使用 MyBatis-Plus 逻辑删除，数据不丢失 |
| **API文档** | SpringDoc OpenAPI 自动生成 Swagger 接口文档 |

---

## 四、数据库 E-R 关系

```
user (用户)
  ├── 1:1 ── runner (跑腿员档案)
  ├── 1:N ── task (发布的任务)
  ├── 1:N ── order (关联的订单 — 作为发布者)
  ├── 1:N ── message (发送/接收的消息)
  ├── 1:N ── notification (接收的通知)
  └── 1:N ── transaction_record (交易流水)

runner (跑腿员)
  ├── N:1 ── user (关联用户)
  ├── 1:N ── order (接的订单)
  └── 1:N ── settlement (结算记录)

task (任务)
  ├── N:1 ── user (发布者)
  └── 1:1 ── order (被抢后生成订单)

order (订单)
  ├── N:1 ── task (关联任务)
  ├── N:1 ── runner (跑腿员)
  ├── N:1 ── user (发布用户)
  ├── 1:N ── order_log (状态日志)
  ├── 1:N ── evaluation (双方评价)
  └── 1:1 ── settlement (结算记录)
```

---

## 五、答辩记录

以下按模块划分，每个模块准备 **3~5 个答辩问题**，涵盖基础知识、业务逻辑和技术实现三个维度。

---

### 学生 1（负责用户管理 + 快递点管理模块）

**Q1：用户密码是如何存储的？为什么不用明文？**
> 答：密码使用 MD5 哈希后存储到数据库。不用明文是因为如果数据库泄露，明文密码会直接暴露，使用哈希即使数据库泄露也无法反推出原始密码，保障用户账号安全。

**Q2：什么是逻辑删除？项目中如何实现逻辑删除？**
> 答：逻辑删除是指不真正从数据库中删除数据，而是通过一个标记字段（deleted=1）来表示数据已删除。项目中所有表都有 `deleted` 字段（0=正常，1=已删除），MyBatis-Plus 自动在查询时过滤掉 `deleted=1` 的记录。

**Q3：用户头像上传支持哪两种方式？文件存在哪里？**
> 答：支持文件上传（MultipartFile）和 Base64 编码上传两种方式。文件存储在服务器本地磁盘 `C:/images` 目录下，文件名使用时间戳 + UUID 保证唯一性。

---

### 学生 2（负责跑腿员管理 + 任务发布模块）

**Q1：普通人如何成为跑腿员？需要经过什么流程？**
> 答：普通用户在个人中心点击"申请成为跑腿员"，填写真实姓名和手机号提交申请。申请后跑腿员状态为待审核（audit_status=0），管理员在后台审核通过后（audit_status=1），该用户才具有跑腿员接单权限。

**Q2：任务表中的 version 字段是干什么的？**
> 答：version 是乐观锁版本号。当跑腿员抢单时，先读取任务的当前 version，更新时加上 `WHERE version = 旧值` 条件，并将 version+1。如果两个跑腿员同时抢同一单，只有第一个执行的 SQL 能成功，后面的会因为 version 不匹配而失败，从而防止并发抢单问题。

**Q3：用户发布任务时需要填写哪些信息？**
> 答：需要选择快递点名称、输入取件码、填写送达地址（默认取宿舍楼栋）、设置跑腿费金额、选择是否加急、填写备注说明。

---

### 学生 3（负责订单管理 + 订单日志模块）

**Q1：订单有哪几种状态？请描述完整的状态流转过程。**
> 答：订单共6种状态：0=已接单（抢单成功）→ 1=已取件（跑腿员取到快递）→ 2=配送中（开始配送）→ 3=已送达（快递到达）→ 4=待评价（用户确认收货）→ 5=已完成（双方互评后）。每次状态变更都会自动记录到 order_log 表和推送通知。

**Q2：抢单接口做了哪些安全校验？**
> 答：（1）校验任务是否存在；（2）校验任务是否处于待接单状态（status=0）；（3）校验跑腿员不能抢自己发布的任务；（4）校验跑腿员档案是否存在；（5）校验跑腿员审核是否通过；（6）校验跑腿员是否被禁用。

**Q3：订单日志表记录了什么信息？有什么作用？**
> 答：记录每次状态变更的订单ID、变更前状态（from_status）、变更后状态（to_status）、操作人ID、操作人角色（用户/跑腿员/管理员）和备注。作用是追踪订单的完整生命周期，便于问题排查和审计。

---

### 学生 4（负责评价 + 自动评价模块）

**Q1：评价是如何触发结算的？描述完整的评价→结算流程。**
> 答：用户确认收货后订单进入待评价状态。当用户评价跑腿员后（或系统自动评价后），系统自动创建结算记录，将跑腿费从用户余额转入跑腿员余额，同时记录交易流水、更新跑腿员统计（订单数、总收入、平均评分）。双方都评价后订单状态变为已完成。

**Q2：自动评价服务是如何工作的？**
> 答：使用 Spring `@Scheduled` 注解，每30秒执行一次。扫描所有已送达（status=3/4）且送达时间超过1分钟的订单，检查用户是否已评价，若未评价则自动创建5星好评（内容："很好，不错"），然后触发结算和订单完成流程。

**Q3：评价表为什么用 `uk_order_from_type` 唯一索引？**
> 答：这是 `(order_id, from_user_id, type)` 的联合唯一索引，确保同一个订单中，同一个评价人对同一评价类型只能评价一次，防止用户给同一个跑腿员重复评价。

---

### 学生 5（负责结算 + 交易流水模块）

**Q1：结算表有哪些状态？各代表什么含义？**
> 答：0=待结算（评价完成但尚未打款），1=已结算（跑腿费已转入跑腿员账户），2=已退款（跑腿费已退回用户账户）。正常情况下结算记录创建后自动变为已结算状态。

**Q2：交易流水表记录了哪些交易类型？**
> 答：0=充值（用户给自己充值），1=支付跑腿费（用户支付任务费用），2=收入跑腿费（跑腿员完成任务获得收入），3=提现（跑腿员提取余额），4=退款（费用退回用户）。每条流水记录交易金额和交易后余额。

**Q3：结算时如何更新跑腿员的统计数据？**
> 答：结算时更新跑腿员的三个统计字段：（1）order_count 累计接单数 +1；（2）total_income 累计收益加上本次跑腿费；（3）score 平均评分重新计算（所有评价分数的加权平均值，保留两位小数）。

---

### 学生 6（负责消息 + 通知模块）

**Q1：消息表和通知表有什么区别？**
> 答：消息表（message）是用户之间主动发送的私信，有发送人和接收人，支持两人之间的聊天记录查询。通知表（notification）是系统自动推送的消息（如订单状态变更、评价提醒等），由后端在业务逻辑中自动创建，用户只能接收不能回复。

**Q2：前端如何实现未读消息数量的实时更新？**
> 答：前端在 MainLayout 组件中使用 `setInterval` 每30秒轮询一次，调用通知未读数量接口和消息未读接口，将两者数量合并显示在顶部栏的角标上。侧边栏的消息通知菜单只显示系统通知的未读数。

**Q3：通知有哪些类型？分别在什么场景触发？**
> 答：主要类型包括：TASK_ASSIGNED（任务被接单时通知发布者）、STATUS_CHANGED（订单状态变更时通知双方）、SETTLEMENT（结算完成时通知跑腿员）、ANNOUNCEMENT（系统公告）。这些通知在 OrderController 的各个业务方法中自动创建并保存。

---

### 学生 7（负责前端登录/路由/权限 + 抢单大厅）

**Q1：前端路由守卫做了哪些校验？**
> 答：在 `router.beforeEach` 中做了三层校验：（1）Token校验 — 未登录跳转登录页；（2）用户信息校验 — userInfo 损坏时清空状态并跳转登录页；（3）角色权限校验 — 根据 `meta.roles` 配置检查当前用户角色是否有权访问该页面，无权则跳转到对应首页（普通用户→发布任务、跑腿员→抢单大厅、管理员→数据统计）。

**Q2：前端如何根据不同角色显示不同菜单？**
> 答：在 MainLayout 中定义了 `menuConfig` 对象，按角色（0/1/2）配置不同的菜单项数组。通过 `computed` 属性根据当前登录用户的 role 动态计算 `currentMenu`，菜单使用 `v-for` 遍历渲染。

**Q3：抢单大厅如何实现筛选功能？**
> 答：抢单大厅提供两个筛选维度：按快递点名称筛选（下拉选择框）和按跑腿费区间筛选（最低金额和最高金额输入框）。筛选参数通过 `taskApi.available(params)` 接口传给后端，后端使用 MyBatis-Plus 的 `LambdaQueryWrapper` 动态构建查询条件。

---

### 学生 8（负责管理员端：数据统计 + 用户管理 + 结算管理）

**Q1：数据统计页面展示了哪些指标？数据从哪里来？**
> 答：展示5个核心指标卡片：总用户数/启用数、跑腿员数/已通过数/待审数、总任务数/待接单数/已取消数、总订单数/已完成数/进行中数、结算总额/笔数/待结算数。数据通过分别调用 userApi、runnerApi、taskApi、orderApi、settlementApi 的 list 接口获取 total 字段来统计。

**Q2：管理员审核跑腿员的流程是怎样的？**
> 答：管理员在跑腿员管理页面看到所有跑腿员列表，点击审核按钮，选择通过（auditStatus=1）或拒绝（auditStatus=2），拒绝时可填写审核备注说明原因。审核通过后跑腿员才能接单。

**Q3：用户管理支持哪些操作？**
> 答：支持查看所有用户列表（分页、按学号/昵称搜索）、启用/禁用用户（status=0禁用后无法登录）、查看用户详情和编辑用户信息。

---

### 学生 9（负责前端用户端：发布任务 + 我的任务 + 个人中心）

**Q1：发布任务时前端做了哪些表单校验？**
> 答：（1）快递点不能为空；（2）取件码不能为空；（3）送达地址默认取用户预设的宿舍楼栋；（4）跑腿费默认5元，需保证余额充足；（5）可勾选是否加急。所有校验在前端使用 Element Plus 的表单验证规则实现。

**Q2：我的任务页面如何展示任务的不同状态？**
> 答：使用 Element Plus 的 `<el-tag>` 组件，根据任务 status 显示不同颜色的标签：待接单（蓝色）、已接单（绿色）、已取消（灰色）。待接单状态的任务显示取消按钮。

**Q3：个人中心支持哪些功能？**
> 答：查看和编辑个人信息（昵称、手机号、宿舍楼栋、真实姓名）、上传头像、修改密码、账户充值、申请成为跑腿员。

---

### 学生 10（负责前端跑腿员端：我的接单 + 收益结算 + 评价管理）

**Q1：跑腿员如何更新配送状态？有哪些操作按钮？**
> 答：在我的接单页面，根据订单当前状态显示不同的操作按钮：已接单→"确认取件"按钮 → 已取件→"开始配送"按钮 → 配送中→"确认送达"按钮。每个按钮调用 `orderApi.updateStatus` 接口更新订单状态。

**Q2：收益结算页面展示了哪些信息？**
> 答：展示跑腿员的收益统计（总收益、已完成订单数、平均评分），以及结算明细列表，包含每笔订单的结算金额、结算时间和结算状态。

**Q3：跑腿员如何查看用户给自己的评价？**
> 答：在"我收到的评价"页面，调用 `evaluationApi.getByToUser(userId)` 接口查询所有 type=0（用户评跑腿员）的评价，展示评分星级、文字内容和评价标签。

---

## 六、答辩常见综合问题（补充）

**Q11：项目中使用 MyBatis-Plus 的哪些核心功能？**
> 答：（1）BaseMapper 提供基础 CRUD；（2）LambdaQueryWrapper 实现类型安全的条件查询；（3）分页插件 Page 实现物理分页；（4）逻辑删除 @TableLogic 注解；（5）乐观锁 @Version 注解；（6）自动填充 create_time/update_time。

**Q12：前后端如何通信？数据格式是什么？**
> 答：前端使用 Axios 发送 HTTP 请求，后端提供 RESTful API（`/api/*`）。请求和响应都使用 JSON 格式。前端在 `api/request.ts` 中封装了 Axios 实例，配置了 baseURL、超时时间和统一错误拦截器。

**Q13：如何处理跨域问题？**
> 答：后端在 `WebMvcConfig.java` 中配置 CORS 跨域，允许前端开发服务器（如 localhost:5173）的请求访问后端接口。

**Q14：项目的部署方式是怎样的？**
> 答：后端 Spring Boot 打包为 jar 文件直接运行（内嵌 Tomcat），前端 Vue 项目 `npm run build` 打包为静态文件部署到 Nginx 或直接放在后端 static 目录下。数据库使用本地 MySQL + Redis。

---

> **文档说明**：以上功能模块说明基于代码实际分析整理，答辩问题覆盖了业务理解、技术实现、安全考虑三个维度，每个学生至少3个问题，可根据实际分工调整。
